
options {
    directory "/var/cache/bind";

    listen-on { 172.30.0.51; };
    listen-on-v6 { none; };

    recursion no;

    allow-query { any; };
};

zone "eta.signed" {
    type primary;
    file "/var/lib/bind/db.eta.signed.signed";
};
